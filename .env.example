PORT=3000
DB_PORT=5434
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=EMNSAMBLIA_DB

JWT_SECRET=
JWT_EXPIRES_IN=2h

# Opcional: credenciales de admin fijas (login sin usuario en la tabla usuario)
# ADMIN_USER=
# ADMIN_PASSWORD_HASH=

# Opcional: clave para que un registro por /api/auth/register cree el usuario con es_admin=true
# (se manda como admin_key en el body). Elige tu propia clave, no la compartas en el repo.
# ADMIN_BOOTSTRAP_KEY=
